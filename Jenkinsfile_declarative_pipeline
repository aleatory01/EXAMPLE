pipeline {
  agent any
  stages {
    stage ("Welcome") {
      steps {
        echo "Welcome to Jenkins declaratives"
        sh 'git --version'
        sh 'chmod +x hello.py'
        sh 'hello.py'
      }
    }
  }
}